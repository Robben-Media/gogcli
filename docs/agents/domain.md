# Domain documentation

This repository uses a single domain context.

Before exploring a domain concept, read `CONTEXT.md` when it exists and any relevant ADRs under `docs/adr/`. Their absence is not an error and does not require creating placeholder documentation.

Use vocabulary established by those documents in issues, agent briefs, implementation, and tests. Surface conflicts with an existing ADR rather than silently overriding the recorded decision.
